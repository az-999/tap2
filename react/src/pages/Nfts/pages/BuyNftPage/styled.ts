import styled from 'styled-components/macro';

export const NftShopPageContainer = styled.div`
  display: flex;
  flex-direction: column;
  padding-top: 14px;
  gap: 18px;

  & > button:first-child {
    top: 28px;
    z-index: 5;
  }
`;
