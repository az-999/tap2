apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tap-ton.fullname" . }}-env
  labels:
    {{- include "tap-ton.labels" . | nindent 4 }}
data:
{{- range $k,$v := .Values.env }}
  {{ $k }}: {{ $v | b64enc| quote }}
{{- end }}
