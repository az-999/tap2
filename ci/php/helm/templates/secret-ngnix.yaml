apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tap-php.fullname" . }}-env-nginx
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "tap-php.fullname" $ }}
data:
{{- range $k,$v := .Values.env.nginx }}
  {{ $k }}: {{ $v | toString | b64enc | quote }}
{{- end }}
