#pragma version =0.4.6;
#pragma allow-post-modification;
#pragma compute-asm-ltr;

#include "tact_StakeMaster.headers.fc";
#include "tact_StakeMaster.stdlib.fc";
#include "tact_StakeMaster.storage.fc";

;;
;; Contract StakeMaster functions
;;

(slice, int, slice, slice, slice, cell, int, slice, cell, int) $StakeMaster$_contract_init(slice $owner_address, int $owner_pubkey, slice $jetton_address, slice $nft_address, cell $metadata, slice $fee_address, slice $treasury, int $version) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = (null(), null(), null(), null(), null(), null(), 0, null(), null(), null());
    $self'owner_address = $owner_address;
    $self'jetton_address = $jetton_address;
    $self'metadata = $metadata;
    $self'owner_pubkey = $owner_pubkey;
    $self'fee_address = $fee_address;
    $self'nft_address = $nft_address;
    $self'treasury = $treasury;
    $self'version = $version;
    $self'old_stakes = null();
    return ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), (cell, cell)) $StakeMaster$_fun_getStakeStateInit((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int, int, int, int, slice, slice, int) $msg) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var (($msg'query_id, $msg'amount, $msg'out_amount, $msg'mint_count, $msg'duration, $msg'owner_address, $msg'signature, $msg'max_claims)) = $msg;
    slice $selfAddr = $global_myAddress();
    var $fresh$ret_0 = $Stake$_init_child(__tact_context_sys, $self'next_stake_id, $selfAddr, $msg'owner_address, $self'fee_address, $msg'amount, $msg'out_amount, $msg'duration, $msg'mint_count, $self'metadata, $msg'max_claims);
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_0);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), ()) $StakeMaster$_fun_deposit((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int, int, int, int, slice, slice, int) $msg, int $value, slice $burnAddress) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var (($msg'query_id, $msg'amount, $msg'out_amount, $msg'mint_count, $msg'duration, $msg'owner_address, $msg'signature, $msg'max_claims)) = $msg;
    int $validate = $Cell$_fun_hash($Builder$_fun_endCell(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint($global_beginCell(), $msg'query_id, 64), $msg'amount, 256), $msg'out_amount, 256), $msg'mint_count, 32), $msg'duration, 32), $msg'max_claims, 32)));
    int $signature_valid = $global_checkSignature($validate, $msg'signature, $self'owner_pubkey);
    throw_unless(48401, $signature_valid);
    var ($stateInit'code, $stateInit'data) = ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)~$StakeMaster$_fun_getStakeStateInit($Deposit$_tensor_cast(($msg'query_id, $msg'amount, $msg'out_amount, $msg'mint_count, $msg'duration, $msg'owner_address, $msg'signature, $msg'max_claims)));
    slice $stateInitAddress = $global_contractAddress(($stateInit'code, $stateInit'data));
    $global_send($SendParameters$_constructor_to_value_mode_bounce_code_data($stateInitAddress, $value, 2, false, $stateInit'code, $stateInit'data));
    $self'next_stake_id = ($self'next_stake_id + 1);
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), slice) $StakeMaster$_fun_getJettonAddress((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var $fresh$ret_1 = $self'jetton_address;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_1);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), cell) $StakeMaster$_fun_getMetadata((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var $fresh$ret_2 = $self'metadata;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_2);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), slice) $StakeMaster$_fun_getFeeAddress((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var $fresh$ret_3 = $self'fee_address;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_3);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), slice) $StakeMaster$_fun_getNftAddress((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var $fresh$ret_4 = $self'nft_address;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_4);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), slice) $StakeMaster$_fun_getOwnerAddress((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var $fresh$ret_5 = $self'owner_address;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_5);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), int) $StakeMaster$_fun_getOwnerPubkey((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var $fresh$ret_6 = $self'owner_pubkey;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_6);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), int) $StakeMaster$_fun_nextStakeId((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    var $fresh$ret_7 = $self'next_stake_id;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_7);
}

((slice, int, slice, slice, slice, cell, int, slice, cell, int), tuple) $StakeMaster$_fun_get_old_stake_record((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, int $stake_id, slice $user_address) impure inline_ref {
    var (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)) = $self;
    cell $stake_cell = __tact_dict_get_int_cell($self'old_stakes, 257, $stake_id);
    if (null?($stake_cell)) {
        var $fresh$ret_8 = null();
        return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_8);
    }
    slice $sc = $Cell$_fun_beginParse(__tact_not_null($stake_cell));
    int $stored_stake_id = $sc~load_uint(64);
    slice $stored_user_address = $sc~__tact_load_address();
    if (( (($stored_stake_id != $stake_id)) ? (true) : (( ~ __tact_slice_eq_bits($user_address, $stored_user_address) )) )) {
        var $fresh$ret_9 = null();
        return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_9);
    }
    var $fresh$ret_10 = $OldStakeRecord$_as_optional($OldStakeRecord$_constructor_stake_amount_out_amount_mint_count_max_claims_claims_count_stake_time_created_at_next_claim_last_claim_end_time_wallet_address($sc~$Slice$_fun_loadCoins(), $sc~$Slice$_fun_loadCoins(), $sc~load_uint(8), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~__tact_load_address()));
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), $fresh$ret_10);
}

;;
;; Receivers of a Contract StakeMaster
;;

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_Deposit((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int, int, int, int, slice, slice, int) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'amount, $msg'out_amount, $msg'mint_count, $msg'duration, $msg'owner_address, $msg'signature, $msg'max_claims) = $msg;
    var ($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
    int $msgValue = $ctx'value;
    throw_unless(53983, ($msgValue >= (10000000 * $msg'mint_count)));
    throw_unless(61734, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    int $tonBalanceBeforeMsg = ($global_myBalance() - $msgValue);
    int $storageFee = (20000000 - $global_min($tonBalanceBeforeMsg, 20000000));
    $msgValue = ($msgValue - ($storageFee + 20000000));
    ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)~$StakeMaster$_fun_deposit(($msg'query_id, $msg'amount, $msg'out_amount, $msg'mint_count, $msg'duration, $msg'owner_address, $msg'signature, $msg'max_claims), $msgValue, $self'jetton_address);
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_DoMint((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, slice, slice, slice, int, int, slice, int) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'receiver, $msg'sender, $msg'signature, $msg'mint_count, $msg'amount, $msg'wallet_address, $msg'fees) = $msg;
    throw_unless(4429, ( __tact_slice_eq_bits($msg'sender, __tact_context_get_sender()) ));
    int $validate = $Cell$_fun_hash($Builder$_fun_endCell(__tact_store_address(__tact_store_address(__tact_store_address(store_uint($global_beginCell(), $msg'query_id, 64), $msg'receiver), $msg'sender), $msg'wallet_address)));
    int $signature_valid = $global_checkSignature($validate, $msg'signature, $self'owner_pubkey);
    throw_unless(48401, $signature_valid);
    var ($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
    int $msgValue = $ctx'value;
    int $requiredForMint = ($msg'mint_count * 11000000);
    throw_unless(5165, ($msgValue >= $requiredForMint));
    if (($msg'mint_count > 0)) {
        int $i = 0;
        cell $mintDict = new_dict();
        while (($i < $msg'mint_count)) {
            int $index = $global_random(0, 6);
            cell $metadata = __tact_not_null(__tact_dict_get_int_cell($self'metadata, 257, $index));
            cell $nftContentCell = $Builder$_fun_endCell(__tact_store_address($Builder$_fun_storeRef(__tact_store_address($global_beginCell(), $msg'receiver), $metadata), $self'treasury));
            cell $itemCell = $Builder$_fun_endCell($Builder$_fun_storeRef($Builder$_fun_storeCoins($global_beginCell(), 10000000), $nftContentCell));
            $mintDict = udict_set($mintDict, 64, $i, $Cell$_fun_asSlice($itemCell));
            $i = ($i + 1);
        }
        if (__tact_cell_neq($global_emptyCell(), $mintDict)) {
            cell $mintMsg = $Builder$_fun_endCell($Builder$_fun_storeRef(store_uint(store_uint($global_beginCell(), 2, 32), $msg'query_id, 64), $mintDict));
            int $mintValue = ((11000000 * $msg'mint_count) + 50000000);
            $global_send($SendParameters$_constructor_to_value_mode_bounce_body($self'nft_address, $mintValue, 1, false, $mintMsg));
            $msgValue = ($msgValue - $mintValue);
        }
    }
    cell $transferMsg = $Builder$_fun_endCell(__tact_store_bool($Builder$_fun_storeCoins(__tact_store_bool(store_uint(__tact_store_address($Builder$_fun_storeCoins(store_uint(store_uint($global_beginCell(), 260734629, 32), $msg'query_id, 64), $msg'amount), $msg'receiver), 0, 2), false), 0), false));
    $global_send($SendParameters$_constructor_to_value_mode_bounce_body($msg'wallet_address, 50000000, 1, false, $transferMsg));
    $msgValue = ((($msgValue - 40000000) - 50000000) - $Context$_fun_readForwardFee(($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw)));
    int $balance = $global_myBalance();
    if (($balance < $msgValue)) {
        $msgValue = 0;
    }
    if (($msgValue > 0)) {
        $global_send($SendParameters$_constructor_to_value_mode_bounce_body($self'fee_address, $msgValue, 2, false, $Claimed$_store_cell($Claimed$_constructor_query_id($msg'query_id))));
    }
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_TokenExcesses((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'queryId) = $msg;
    var ($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
    int $msgValue = $ctx'value;
    $global_send($SendParameters$_constructor_to_value_mode_bounce_body($self'owner_address, $msgValue, 2, false, $TokenExcesses$_store_cell(($msg'queryId))));
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_TokenNotification((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int, slice, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'amount, $msg'from, $msg'forward_payload) = $msg;
    var ($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
    int $msgValue = $ctx'value;
    var ($depositMsg'query_id, $depositMsg'amount, $depositMsg'out_amount, $depositMsg'mint_count, $depositMsg'duration, $depositMsg'owner_address, $depositMsg'signature, $depositMsg'max_claims) = $Deposit$_load_not_mut($Slice$_fun_loadRef$not_mut($Cell$_fun_beginParse($msg'forward_payload~$Slice$_fun_loadRef())).begin_parse());
    $depositMsg'owner_address = $msg'from;
    $depositMsg'amount = $msg'amount;
    ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version)~$StakeMaster$_fun_deposit(($depositMsg'query_id, $depositMsg'amount, $depositMsg'out_amount, $depositMsg'mint_count, $depositMsg'duration, $depositMsg'owner_address, $depositMsg'signature, $depositMsg'max_claims), ($msgValue - 40000000), $ctx'sender);
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_ProxyMsg((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, slice, int, cell) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'to, $msg'value, $msg'body) = $msg;
    throw_unless(12603, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    $global_send($SendParameters$_constructor_to_mode_value_body_bounce($msg'to, 64, $msg'value, $msg'body, false));
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_UpdateFeeAddress((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'new_fee_address) = $msg;
    throw_unless(59508, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    $self'fee_address = $msg'new_fee_address;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_UpdateNftAddress((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'new_nft_address) = $msg;
    throw_unless(21750, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    $self'nft_address = $msg'new_nft_address;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_UpdateJettonAddress((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'new_jetton_address) = $msg;
    throw_unless(15215, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    $self'jetton_address = $msg'new_jetton_address;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_TransferOwnership((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'new_owner) = $msg;
    throw_unless(62971, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    $self'owner_address = $msg'new_owner;
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) %$StakeMaster$_internal_empty((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    throw_unless(34646, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_AddOldStakeRecord((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int, slice, int, int, int, int, int, int, int, int, int, int, slice, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'stake_id, $msg'user_address, $msg'stake_amount, $msg'out_amount, $msg'mint_count, $msg'max_claims, $msg'claims_count, $msg'stake_time, $msg'created_at, $msg'next_claim, $msg'last_claim, $msg'end_time, $msg'signature, $msg'wallet_address) = $msg;
    throw_unless(14534, ( __tact_slice_eq_bits($self'owner_address, __tact_context_get_sender()) ));
    var ($record'stake_amount, $record'out_amount, $record'mint_count, $record'max_claims, $record'claims_count, $record'stake_time, $record'created_at, $record'next_claim, $record'last_claim, $record'end_time, $record'wallet_address) = $OldStakeRecord$_constructor_stake_amount_out_amount_mint_count_max_claims_claims_count_stake_time_created_at_next_claim_last_claim_end_time_wallet_address($msg'stake_amount, $msg'out_amount, $msg'mint_count, $msg'max_claims, $msg'claims_count, $msg'stake_time, $msg'created_at, $msg'next_claim, $msg'last_claim, $msg'end_time, $msg'wallet_address);
    cell $stake_cell = $Builder$_fun_endCell(__tact_store_address(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint($Builder$_fun_storeCoins($Builder$_fun_storeCoins(__tact_store_address(store_uint($global_beginCell(), $msg'stake_id, 64), $msg'user_address), $record'stake_amount), $record'out_amount), $record'mint_count, 8), $record'max_claims, 32), $record'claims_count, 32), $record'stake_time, 32), $record'created_at, 32), $record'next_claim, 32), $record'last_claim, 32), $record'end_time, 32), $record'wallet_address));
    $self'old_stakes~__tact_dict_set_int_cell(257, $msg'stake_id, $stake_cell);
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_ClaimOldStake((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int, slice, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'stake_id, $msg'wallet_address, $msg'signature) = $msg;
    var ($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
    int $msgValue = $ctx'value;
    cell $stake_cell = __tact_dict_get_int_cell($self'old_stakes, 257, $msg'stake_id);
    throw_unless(2512, (~ null?($stake_cell)));
    slice $sc = $Cell$_fun_beginParse(__tact_not_null($stake_cell));
    int $stake_id = $sc~load_uint(64);
    slice $user_address = $sc~__tact_load_address();
    throw_unless(4429, ( __tact_slice_eq_bits($user_address, __tact_context_get_sender()) ));
    int $validate = $Cell$_fun_hash($Builder$_fun_endCell(__tact_store_address(__tact_store_address(store_uint(store_uint($global_beginCell(), $msg'query_id, 64), $msg'stake_id, 64), __tact_context_get_sender()), $msg'wallet_address)));
    int $signature_valid = $global_checkSignature($validate, $msg'signature, $self'owner_pubkey);
    throw_unless(48401, $signature_valid);
    var ($record'stake_amount, $record'out_amount, $record'mint_count, $record'max_claims, $record'claims_count, $record'stake_time, $record'created_at, $record'next_claim, $record'last_claim, $record'end_time, $record'wallet_address) = $OldStakeRecord$_constructor_stake_amount_out_amount_mint_count_max_claims_claims_count_stake_time_created_at_next_claim_last_claim_end_time_wallet_address($sc~$Slice$_fun_loadCoins(), $sc~$Slice$_fun_loadCoins(), $sc~load_uint(8), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~__tact_load_address());
    throw_unless(63694, ($record'next_claim < $global_now()));
    throw_unless(45220, ($record'claims_count < $record'max_claims));
    int $requiredForMint = (((($record'mint_count * 12500000) + 20000000) + 20000000) + 50000000);
    throw_unless(22565, ($msgValue >= $requiredForMint));
    int $fees = ($msgValue - $requiredForMint);
    if (($fees < 0)) {
        $fees = 0;
    }
    cell $transferMsg = $Builder$_fun_endCell(__tact_store_bool($Builder$_fun_storeCoins(__tact_store_bool(store_uint(__tact_store_address($Builder$_fun_storeCoins(store_uint(store_uint($global_beginCell(), 260734629, 32), $msg'query_id, 64), $record'out_amount), $record'wallet_address), 0, 2), false), 0), false));
    $global_send($SendParameters$_constructor_to_value_mode_bounce_body($msg'wallet_address, 50000000, 1, false, $transferMsg));
    if (($record'mint_count > 0)) {
        cell $mintDict = new_dict();
        int $i = 0;
        while (($i < $record'mint_count)) {
            int $index = $global_random(0, 6);
            cell $metadata = __tact_not_null(__tact_dict_get_int_cell($self'metadata, 257, $index));
            cell $nftContentCell = $Builder$_fun_endCell(__tact_store_address($Builder$_fun_storeRef(__tact_store_address($global_beginCell(), __tact_context_get_sender()), $metadata), $self'treasury));
            cell $itemCell = $Builder$_fun_endCell($Builder$_fun_storeRef($Builder$_fun_storeCoins($global_beginCell(), 10000000), $nftContentCell));
            $mintDict = udict_set($mintDict, 64, $i, $Cell$_fun_asSlice($itemCell));
            $i = ($i + 1);
        }
        cell $mintMsg = $Builder$_fun_endCell($Builder$_fun_storeRef(store_uint(store_uint($global_beginCell(), 2, 32), $msg'query_id, 64), $mintDict));
        int $mintValue = ((11000000 * $record'mint_count) + 50000000);
        $global_send($SendParameters$_constructor_to_value_mode_bounce_body($self'nft_address, $mintValue, 1, false, $mintMsg));
        $msgValue = ($msgValue - $mintValue);
    }
    $msgValue = (($msgValue - 40000000) - $Context$_fun_readForwardFee(($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw)));
    if (($msgValue > 0)) {
        $global_send($SendParameters$_constructor_to_value_mode_bounce_body($self'fee_address, $msgValue, 2, false, $Claimed$_store_cell($Claimed$_constructor_query_id($msg'query_id))));
    }
    $record'claims_count = ($record'claims_count + 1);
    $record'next_claim = ($record'next_claim + $record'stake_time);
    cell $updated_cell = $Builder$_fun_endCell(__tact_store_address(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint($Builder$_fun_storeCoins($Builder$_fun_storeCoins(__tact_store_address(store_uint($global_beginCell(), $msg'stake_id, 64), __tact_context_get_sender()), $record'stake_amount), $record'out_amount), $record'mint_count, 8), $record'max_claims, 32), $record'claims_count, 32), $record'stake_time, 32), $record'created_at, 32), $record'next_claim, 32), $record'last_claim, 32), $record'end_time, 32), $record'wallet_address));
    $self'old_stakes~__tact_dict_set_int_cell(257, $msg'stake_id, $updated_cell);
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_RestakeOldStake((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'stake_id) = $msg;
    slice $sender = $Context$_get_sender(__tact_context_get());
    cell $stake_cell = __tact_dict_get_int_cell($self'old_stakes, 257, $msg'stake_id);
    throw_unless(2512, (~ null?($stake_cell)));
    slice $sc = $Cell$_fun_beginParse(__tact_not_null($stake_cell));
    int $stake_id = $sc~load_uint(64);
    slice $user_address = $sc~__tact_load_address();
    throw_unless(20993, ( __tact_slice_eq_bits(__tact_context_get_sender(), $user_address) ));
    var ($record'stake_amount, $record'out_amount, $record'mint_count, $record'max_claims, $record'claims_count, $record'stake_time, $record'created_at, $record'next_claim, $record'last_claim, $record'end_time, $record'wallet_address) = $OldStakeRecord$_constructor_stake_amount_out_amount_mint_count_max_claims_claims_count_stake_time_created_at_next_claim_last_claim_end_time_wallet_address($sc~$Slice$_fun_loadCoins(), $sc~$Slice$_fun_loadCoins(), $sc~load_uint(8), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~__tact_load_address());
    throw_unless(14562, ($record'claims_count >= $record'max_claims));
    $record'claims_count = 0;
    $record'next_claim = ($global_now() + $record'stake_time);
    cell $updated_cell = $Builder$_fun_endCell(__tact_store_address(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint(store_uint($Builder$_fun_storeCoins($Builder$_fun_storeCoins(__tact_store_address(store_uint($global_beginCell(), $msg'stake_id, 64), __tact_context_get_sender()), $record'stake_amount), $record'out_amount), $record'mint_count, 8), $record'max_claims, 32), $record'claims_count, 32), $record'stake_time, 32), $record'created_at, 32), $record'next_claim, 32), $record'last_claim, 32), $record'end_time, 32), $record'wallet_address));
    $self'old_stakes~__tact_dict_set_int_cell(257, $msg'stake_id, $updated_cell);
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

(((slice, int, slice, slice, slice, cell, int, slice, cell, int)), ()) $StakeMaster$_internal_binary_WithdrawOldStake((slice, int, slice, slice, slice, cell, int, slice, cell, int) $self, (int, int, slice, slice) $msg) impure inline {
    var ($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version) = $self;
    var ($msg'query_id, $msg'stake_id, $msg'signature, $msg'wallet_address) = $msg;
    var ($ctx'bounced, $ctx'sender, $ctx'value, $ctx'raw) = __tact_context_get();
    slice $sender = $ctx'sender;
    int $validate = $Cell$_fun_hash($Builder$_fun_endCell(__tact_store_address(__tact_store_address(store_uint($global_beginCell(), $msg'query_id, 64), __tact_context_get_sender()), $msg'wallet_address)));
    int $signature_valid = $global_checkSignature($validate, $msg'signature, $self'owner_pubkey);
    throw_unless(48401, $signature_valid);
    cell $stake_cell = __tact_dict_get_int_cell($self'old_stakes, 257, $msg'stake_id);
    throw_unless(2512, (~ null?($stake_cell)));
    slice $sc = $Cell$_fun_beginParse(__tact_not_null($stake_cell));
    int $stake_id = $sc~load_uint(64);
    slice $user_address = $sc~__tact_load_address();
    throw_unless(26825, ( __tact_slice_eq_bits(__tact_context_get_sender(), $user_address) ));
    var ($record'stake_amount, $record'out_amount, $record'mint_count, $record'max_claims, $record'claims_count, $record'stake_time, $record'created_at, $record'next_claim, $record'last_claim, $record'end_time, $record'wallet_address) = $OldStakeRecord$_constructor_stake_amount_out_amount_mint_count_max_claims_claims_count_stake_time_created_at_next_claim_last_claim_end_time_wallet_address($sc~$Slice$_fun_loadCoins(), $sc~$Slice$_fun_loadCoins(), $sc~load_uint(8), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~load_uint(32), $sc~__tact_load_address());
    throw_unless(14562, ($record'claims_count >= $record'max_claims));
    cell $transferMsg = $Builder$_fun_endCell(__tact_store_bool($Builder$_fun_storeCoins(__tact_store_bool(store_uint(__tact_store_address($Builder$_fun_storeCoins(store_uint(store_uint($global_beginCell(), 260734629, 32), $msg'query_id, 64), $record'stake_amount), $record'wallet_address), 0, 2), false), 0), false));
    $global_send($SendParameters$_constructor_to_value_mode_bounce_body($msg'wallet_address, 50000000, 1, false, $transferMsg));
    $self'old_stakes~__tact_dict_set_int_cell(257, $msg'stake_id, null());
    return (($self'owner_address, $self'owner_pubkey, $self'nft_address, $self'jetton_address, $self'fee_address, $self'metadata, $self'next_stake_id, $self'treasury, $self'old_stakes, $self'version), ());
}

;;
;; Get methods of a Contract StakeMaster
;;

_ %getStakeStateInit(tuple $msg) method_id(115147) {
    (int, int, int, int, int, slice, slice, int) $msg = $Deposit$_from_tuple($msg);
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_getStakeStateInit($msg);
    return $StateInit$_to_external(res);
}

_ %getJettonAddress() method_id(82160) {
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_getJettonAddress();
    return res;
}

_ %getMetadata() method_id(91290) {
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_getMetadata();
    return res;
}

_ %getFeeAddress() method_id(115192) {
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_getFeeAddress();
    return res;
}

_ %getNftAddress() method_id(122000) {
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_getNftAddress();
    return res;
}

_ %getOwnerAddress() method_id(91429) {
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_getOwnerAddress();
    return res;
}

_ %getOwnerPubkey() method_id(87083) {
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_getOwnerPubkey();
    return res;
}

_ %nextStakeId() method_id(92018) {
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_nextStakeId();
    return res;
}

_ %get_old_stake_record(int $stake_id, slice $user_address) method_id(123076) {
    int $stake_id = $stake_id;
    slice $user_address = __tact_verify_address($user_address);
    var self = $StakeMaster$_contract_load();
    var res = self~$StakeMaster$_fun_get_old_stake_record($stake_id, $user_address);
    return $OldStakeRecord$_to_opt_external(res);
}

_ lazy_deployment_completed() method_id {
    return get_data().begin_parse().load_int(1);
}

;;
;; Routing of a Contract StakeMaster
;;

((slice, int, slice, slice, slice, cell, int, slice, cell, int), int) $StakeMaster$_contract_router_internal((slice, int, slice, slice, slice, cell, int, slice, cell, int) self, int msg_bounced, slice in_msg) impure inline_ref {
    ;; Handle bounced messages
    if (msg_bounced) {
        return (self, true);
    }
    
    ;; Parse incoming message
    int op = 0;
    if (slice_bits(in_msg) >= 32) {
        op = in_msg.preload_uint(32);
    }
    
    
    ;; Receive Deposit message
    if (op == 69877033) {
        var msg = in_msg~$Deposit$_load();
        self~$StakeMaster$_internal_binary_Deposit(msg);
        return (self, true);
    }
    
    ;; Receive DoMint message
    if (op == 2793121783) {
        var msg = in_msg~$DoMint$_load();
        self~$StakeMaster$_internal_binary_DoMint(msg);
        return (self, true);
    }
    
    ;; Receive TokenExcesses message
    if (op == 0xd53276db) {
        var msg = in_msg~$TokenExcesses$_load();
        self~$StakeMaster$_internal_binary_TokenExcesses(msg);
        return (self, true);
    }
    
    ;; Receive TokenNotification message
    if (op == 0x7362d09c) {
        var msg = in_msg~$TokenNotification$_load();
        self~$StakeMaster$_internal_binary_TokenNotification(msg);
        return (self, true);
    }
    
    ;; Receive ProxyMsg message
    if (op == 3891826139) {
        var msg = in_msg~$ProxyMsg$_load();
        self~$StakeMaster$_internal_binary_ProxyMsg(msg);
        return (self, true);
    }
    
    ;; Receive UpdateFeeAddress message
    if (op == 130433518) {
        var msg = in_msg~$UpdateFeeAddress$_load();
        self~$StakeMaster$_internal_binary_UpdateFeeAddress(msg);
        return (self, true);
    }
    
    ;; Receive UpdateNftAddress message
    if (op == 3529429755) {
        var msg = in_msg~$UpdateNftAddress$_load();
        self~$StakeMaster$_internal_binary_UpdateNftAddress(msg);
        return (self, true);
    }
    
    ;; Receive UpdateJettonAddress message
    if (op == 2933033334) {
        var msg = in_msg~$UpdateJettonAddress$_load();
        self~$StakeMaster$_internal_binary_UpdateJettonAddress(msg);
        return (self, true);
    }
    
    ;; Receive TransferOwnership message
    if (op == 1800991741) {
        var msg = in_msg~$TransferOwnership$_load();
        self~$StakeMaster$_internal_binary_TransferOwnership(msg);
        return (self, true);
    }
    
    ;; Receive empty message
    if ((op == 0) & (slice_bits(in_msg) <= 32)) {
        self~%$StakeMaster$_internal_empty();
        return (self, true);
    }
    
    ;; Receive AddOldStakeRecord message
    if (op == 2363339442) {
        var msg = in_msg~$AddOldStakeRecord$_load();
        self~$StakeMaster$_internal_binary_AddOldStakeRecord(msg);
        return (self, true);
    }
    
    ;; Receive ClaimOldStake message
    if (op == 1210333798) {
        var msg = in_msg~$ClaimOldStake$_load();
        self~$StakeMaster$_internal_binary_ClaimOldStake(msg);
        return (self, true);
    }
    
    ;; Receive RestakeOldStake message
    if (op == 4053056756) {
        var msg = in_msg~$RestakeOldStake$_load();
        self~$StakeMaster$_internal_binary_RestakeOldStake(msg);
        return (self, true);
    }
    
    ;; Receive WithdrawOldStake message
    if (op == 974840603) {
        var msg = in_msg~$WithdrawOldStake$_load();
        self~$StakeMaster$_internal_binary_WithdrawOldStake(msg);
        return (self, true);
    }
    
    return (self, false);
}

() recv_internal(int msg_value, cell in_msg_cell, slice in_msg) impure {
    
    ;; Context
    var cs = in_msg_cell.begin_parse();
    var msg_flags = cs~load_uint(4);
    var msg_bounced = -(msg_flags & 1);
    slice msg_sender_addr = __tact_verify_address(cs~load_msg_addr());
    __tact_context = (msg_bounced, msg_sender_addr, msg_value, cs);
    __tact_context_sender = msg_sender_addr;
    
    ;; Load contract data
    var self = $StakeMaster$_contract_load();
    
    ;; Handle operation
    int handled = self~$StakeMaster$_contract_router_internal(msg_bounced, in_msg);
    
    ;; Throw if not handled
    throw_unless(130, handled);
    
    ;; Persist state
    $StakeMaster$_contract_store(self);
}
